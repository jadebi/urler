user urler;
error_log /dev/stderr error;

worker_processes 1;

env DATA_FOLDER;
env BASE_URL;
env PORT;
env LOG_FORMAT;
env DEBUG;

events {
  worker_connections 1024;
}

http {
  access_log /dev/stdout combined;
  access_log off;
  log_not_found off;

  limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;

  error_page 429 = @too_many_requests;

  init_by_lua_block {
    log = require("utils.logging")
    ENV = require("utils.env")
    log.info(string.format("Server running on %s:%s", ENV.BaseUrl, ENV.Port))
  }

  server {
    listen ${PORT};
    server_name ${SERVER_NAME};
    log_by_lua_block {
      -- local log = require("utils.logging")
      log.info(string.format("%s %s %s", ngx.var.remote_addr, ngx.req.get_method(), ngx.var.request_uri))
    }

    location / {
      root /urler/www;
    }

    location = /greetings {
      default_type text/html;
      content_by_lua_block {
        local headers = ngx.req.get_headers()
        ngx.say("Hello Mister " .. ngx.req.get_headers()["user-agent"])
      }
    }

    location = /api/shorten {
      limit_req zone=api;
      limit_req_status 429;
      default_type application/json;
      content_by_lua_file /urler/api/shorten.lua;
    }

    location @too_many_requests {
      default_type application/json;
      return 429 "{\"error\":\"too many requests\"}";
    }
  }
}
